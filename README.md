# Plug TV — Initial repository

This repository contains the Plug TV project scaffold generated by GitHub Copilot.

This initial commit creates a minimal README so the repository is no longer empty. The full scaffold (Next.js 15 + TypeScript + Tailwind + Supabase + PWA + SEO + CI) will be committed to a new branch named `feature/initial-scaffold`.

See the pull request once it's opened for details and next steps.
